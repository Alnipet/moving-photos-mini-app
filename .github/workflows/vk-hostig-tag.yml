name: Deploy to VK Apps

'on':
  push:
    tags:
      - '*'
jobs:
  production_deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: npm i
      - name: VK deploy
        run: npm run deploy
